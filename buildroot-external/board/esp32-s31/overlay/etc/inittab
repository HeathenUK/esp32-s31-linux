::sysinit:/bin/mount -t proc proc /proc
::sysinit:/bin/mkdir -p /dev/pts /dev/shm
::sysinit:/bin/mount -a
::sysinit:/bin/mkdir -p /run/lock/subsys
::sysinit:/bin/hostname -F /etc/hostname
::sysinit:/etc/init.d/rcS
# Restore the interactive serial login shell on the Linux console.
# /dev/console is the LCD (tty0), so name the UART explicitly here or
# the serial port would lose its login shell.
#
# Baud 0 means "keep whatever speed the line is already at". Naming a rate here
# makes getty reprogram the UART after boot, silently undoing the console= rate
# the kernel set: the whole boot log arrives at the new speed and then the login
# prompt appears at the old one.
ttyS0::respawn:/sbin/getty -L ttyS0 0 vt100
# Login on the LCD's virtual terminal. Needs a USB keyboard (HID + evdev).
tty1::respawn:/sbin/getty -L tty1 38400 linux
::shutdown:/etc/init.d/rcK
::shutdown:/sbin/swapoff -a
::shutdown:/bin/umount -a -r
